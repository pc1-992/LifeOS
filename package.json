{
  "name": "lifeos",
  "version": "0.1.0",
  "private": true,
  "description": "A modular intelligent life operating system with context awareness, agents, routines, memory, semantic understanding, and privacy-by-context.",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel --if-present dev",
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=22"
  }
}
